import 'package:flutter/material.dart';

class MyProgressPage extends StatelessWidget {
  const MyProgressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Progress Page'),
    );
  }
}
